# Intelligence Backend

Run API:

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
```

Includes production-oriented engines for crawling, embeddings/Qdrant indexing, entity graph extraction with Neo4j, SEO/AEO/GEO scoring, and AI visibility simulation.
